node_modules
# Keep environment variables out of version control
.env

# Firebase Admin JSON (FIREBASE_SERVICE_ACCOUNT_PATH) — do not commit
firebase-service-account.json
*-firebase-service-account.json

# generated Prisma client
prisma/generated

# Build output (do not commit; run `pnpm build` locally / in CI)
dist

/src/generated/prisma

/uploads
